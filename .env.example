# URL base da API REST. Hoje a aplicação usa um mock local (src/services/api.ts);
# defina esta variável para apontar a um backend real no futuro.
# Copie este arquivo para `.env` e ajuste o valor.
VITE_API_URL=http://localhost:3001
